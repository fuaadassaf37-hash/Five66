# النشر المجاني — Render + Supabase

هذا الدليل يشغّل التطبيق **مجاناً بالكامل** مع تخزين دائم للبيانات (لا تُحذف عند إعادة التشغيل).

---

## الجزء 1: إنشاء قاعدة بيانات Supabase (مجانية)

1. اذهب إلى https://supabase.com وسجّل دخول (يمكن بحساب Google).
2. اضغط **New Project**:
   - اختر اسم للمشروع (مثلاً `diwan`).
   - اختر كلمة مرور لقاعدة البيانات (احفظها في مكان آمن، لن تحتاجها هنا لكن يطلبها).
   - اختر أقرب منطقة (Region) لك (مثلاً Frankfurt أو أي منطقة أوروبية قريبة).
   - اضغط **Create new project** وانتظر دقيقة حتى يجهز.

3. من القائمة اليسرى اذهب إلى **SQL Editor** → **New query**.
   - افتح ملف `supabase_setup.sql` الموجود في هذه الحزمة، انسخ محتواه بالكامل، الصقه، واضغط **Run**.
   - هذا ينشئ الجدول `kv_store` الذي يخزّن كل بيانات التطبيق.

4. اذهب إلى **Project Settings** (⚙️ في الأسفل) → **API**:
   - انسخ القيمة **Project URL** → هذه هي `SUPABASE_URL`.
   - انسخ القيمة **service_role** (تحت API keys، وليس anon) → هذه هي `SUPABASE_KEY`.
   - ⚠ احتفظ بهذا المفتاح بسرّية، لا تشاركه مع أي شخص.

احفظ هاتين القيمتين، ستحتاجهما بالخطوة القادمة.

---

## الجزء 2: نشر السيرفر على Render (مجاني)

1. ضع مجلد `server` في مستودع GitHub (يمكنك إنشاء مستودع جديد على github.com ورفع الملفات عبر الموقع مباشرة "Add file → Upload files").

2. اذهب إلى https://render.com وسجّل دخول (يمكن بحساب GitHub).

3. اضغط **New** → **Web Service**.
4. اختر المستودع الذي رفعت إليه الملفات.
5. الإعدادات:
   - **Name**: أي اسم (مثلاً `diwan-askari`)
   - **Region**: أقرب منطقة
   - **Branch**: main
   - **Root Directory**: إذا رفعت مجلد `server` فقط كجذر المستودع اتركه فارغاً، وإذا رفعت كل الحزمة بمجلداتها ضع `server`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: Free

6. قبل الضغط على Create، افتح قسم **Environment Variables** وأضف:
   | Key | Value |
   |---|---|
   | `APP_USER` | اسم المستخدم الذي تريده |
   | `APP_PASS` | كلمة المرور التي تريدها |
   | `SUPABASE_URL` | القيمة التي نسختها من Supabase |
   | `SUPABASE_KEY` | القيمة التي نسختها من Supabase (service_role) |

7. اضغط **Create Web Service** وانتظر حتى ينتهي البناء (Build) — يستغرق دقيقتين تقريباً.

8. بعد الانتهاء، سيعطيك Render رابطاً مثل:
```
https://diwan-askari.onrender.com
```
هذا هو رابط التطبيق — افتحه من أي متصفح وسيطلب اسم المستخدم وكلمة المرور.

---

## ملاحظات عن النسخة المجانية من Render

- السيرفر "ينام" تلقائياً بعد ~15 دقيقة من عدم الاستخدام.
- عند فتح الرابط بعد فترة نوم، أول تحميل يأخذ 30-50 ثانية حتى يستيقظ السيرفر — بعدها يعمل بسرعة طبيعية.
- البيانات **لا تُفقد** عند النوم أو إعادة التشغيل لأنها محفوظة في Supabase.

---

## التحقق من عمل كل شيء

1. افتح الرابط، سجّل دخول.
2. عدّل أي بيانات (مثلاً أضف خصماً) وانتظر ظهور "✓ محفوظ على السيرفر".
3. اذهب إلى Supabase → **Table Editor** → جدول `kv_store`، يجب أن ترى المفاتيح (`mil_khasm`, `mil_persons_edited`...) بقيم محدثة.
4. افتح الرابط من جهاز/متصفح آخر — يجب أن تظهر نفس التعديلات.

---

## إذا أردت بدون Supabase (تخزين محلي فقط)

اترك `SUPABASE_URL` و`SUPABASE_KEY` فارغين — سيعمل التطبيق ويخزّن في ملف `data/state.json` على السيرفر،
لكن على Render المجاني هذا الملف **يُحذف عند إعادة تشغيل السيرفر** (وهذا يحصل بشكل دوري) —
لذا يُنصح بشدة باستخدام Supabase للاحتفاظ بالبيانات بشكل دائم.
